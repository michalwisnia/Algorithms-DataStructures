#include <iostream>
#include <ctime>

int main()
/*class dag
{
    protected:
    int n;
    int **matrix;
public:

    void generate(int n);

    void display();
};
void dag::generate(int n)
{
        this.n = n;
        matrix = new int[n][n];
        int x, y;
        for (x = 0; x < n; x++)
        {

            for (y = 0; y < n; y++)
            {
                matrix[x][y] = 0; std::cout<<"0";
            }
        }
}
void dag::display()
    {
        int x, y;

        for (x = 0; x < n; x++)
        {
            for (y = 0; y < n; y++)
            {
                std::cout << matrix[x][y] << "   ";
            }
            std::cout << "\n";
        }
    }
int main()
{
    int ver=10;
    dag dagg;
    dagg.generate(ver);
    dagg.display();
}
*/
